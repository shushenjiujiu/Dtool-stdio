{
  "name": "dtool-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Visual dataflow programming tool — dtool Studio",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "type-check": "pnpm -r type-check",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
